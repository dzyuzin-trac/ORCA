create table `kcm-avl`(
	`ID`				INT unsigned AUTO_INCREMENT,
	`DAYCODE` 			MEDIUMINT unsigned,
    `TRIP_ID` 			INT unsigned,
    `PATTERN_ID` 		INT unsigned,
    `PATTERN_QUALITY`	TINYINT unsigned,
    `BLK` 				MEDIUMINT unsigned,
    `RTE` 				SMALLINT unsigned,
	`DIR`				CHAR(2),
    `SCH_ST_MIN`		SMALLINT unsigned,
    `OPD_DATE`			DATE,
    `VEHICLE_ID`		MEDIUMINT unsigned,
    `STOP_ID`			MEDIUMINT unsigned,
    `STOP_SEQ`			TINYINT unsigned,
    `STOP_NAME`			VARCHAR(256),
    `SCH_STOP_SEC`		MEDIUMINT unsigned,
    `ACT_STOP_ARR`		MEDIUMINT unsigned,
    `SCH_STOP_TM`		TIME,
    `ACT_STOP_TM`		TIME,
    `DWELL_SEC`			MEDIUMINT unsigned,
    `DOORS_OPEN`		ENUM('Y', 'N'),
	`DOOR_OPEN_SEC`		SMALLINT unsigned,
    `APC_VEH`			ENUM('Y', 'N'),
    `ONS`				TINYINT unsigned,
    `OFFS`				TINYINT unsigned,
    `LOAD`				TINYINT unsigned,
    `POINT_ROLE`		VARCHAR(8),
    #`GPS_LOCATION`		POINT NOT NULL,
    `GPS_LONGITUDE`		FLOAT,
    `GPS_LATITUDE`		FLOAT,
    UNIQUE KEY `OPD_DATE` (`ID`, `OPD_DATE`),
    INDEX (`TRIP_ID`),
    INDEX (`OPD_DATE`),
    INDEX (`SCH_STOP_TM`),
    INDEX (`ACT_STOP_TM`),
    INDEX (`VEHICLE_ID`),
    INDEX (`STOP_ID`),
    INDEX (`RTE`),
    INDEX (`DOORS_OPEN`)#,
    #SPATIAL INDEX (`GPS_LOCATION`)
) ENGINE=InnoDB PARTITION BY RANGE (to_days(OPD_DATE)) (
PARTITION p0 VALUES LESS THAN (to_days('2015-02-20')),
PARTITION p1 VALUES LESS THAN (to_days('2015-02-27')),
PARTITION p2 VALUES LESS THAN (to_days('2015-03-06')),
PARTITION p3 VALUES LESS THAN (to_days('2015-03-13')),
PARTITION p4 VALUES LESS THAN (to_days('2015-03-20')),
PARTITION p5 VALUES LESS THAN (to_days('2015-03-27')),
PARTITION p6 VALUES LESS THAN (to_days('2015-04-03')),
PARTITION p7 VALUES LESS THAN (to_days('2015-04-10')),
PARTITION p8 VALUES LESS THAN MAXVALUE);